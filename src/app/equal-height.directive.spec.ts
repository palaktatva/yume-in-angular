import { EqualHeightDirective } from './equal-height.directive';

describe('EqualHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualHeightDirective();
    expect(directive).toBeTruthy();
  });
});
